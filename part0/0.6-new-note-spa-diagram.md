# Exercise 0.6 - New Note in SPA Diagram

```mermaid
sequenceDiagram
    participant User
    participant Browser
    participant Server

    User->>Browser: Creates a new note

    Browser->>Server: POST /new_note_spa

    Server-->>Browser: 201 Created

    Browser->>Browser: Add note to DOM

    Browser->>Browser: No page reload
```
